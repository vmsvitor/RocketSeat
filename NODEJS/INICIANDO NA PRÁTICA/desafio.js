/* Crie 2 arquivos JavaScript.
   O primeiro irá exportar uma função chamada getFlag()
   que receberá um argumento do tipo string.
   Essa função deverá buscar dentro do array process.argv a flag desejada e retornar o valor da flag

   O segundo irá importar a funçõa e passar a flag desejada.

   Iremos rodar no terminal o segundo arquivo passando as flags --name e --greeting
   oara que seja impressso no terminal a saudação e o nome da pessoa.


*/