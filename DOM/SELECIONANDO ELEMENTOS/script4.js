// querySelector() - pesquisa pelo seletor

// Busca pelo seletor CSS - pega o primeiro que achar
// Este modo é bem versátil

const element = document.querySelector('.one') // meta ou ([scr])  como atributo

console.log(element)